using UnityEngine;

public class FlyingMonsterDetecter : MonsterDetector
{
    //protected override Vector2 GetTargetPosition(Vector2 playerWorldPos)
    //{
    //    return new Vector2(playerWorldPos.x, playerWorldPos.y);
    //}
}
